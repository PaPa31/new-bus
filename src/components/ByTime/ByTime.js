import React from "react";
import classes from "./ByTime.module.css";

const byTime = (props) => {
  return (
    <div className={classes.ByTime}>
      <h1>Хрон</h1>
    </div>
  );
};

export default byTime;
